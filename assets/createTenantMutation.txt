mutation createTenant($houseKey: String!, $tenant: TenantInput!){
	createTenant(houseKey: $houseKey, tenant: $tenant) {
        firstName,
        lastName,
        email,
        tenantState,
        houseId,
        tenantPosition,
        deviceId
    }
}